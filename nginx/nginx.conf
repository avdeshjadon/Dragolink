server {
    listen 80;
    server_name dragolink.duckdns.org; # Replace with your domain

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        # Until SSL is generated, we just proxy HTTP to backend directly
        # Once SSL is generated, you can uncomment the return 301 line and comment proxy_pass
        proxy_pass http://backend:8080;
        proxy_set_header Host $host;
        
        # return 301 https://$host$request_uri;
    }
}

# UNCOMMENT THIS BLOCK AFTER GENERATING SSL
# server {
#     listen 443 ssl;
#     server_name dragolink.duckdns.org; # Replace with your domain
#
#     ssl_certificate /etc/letsencrypt/live/dragolink.duckdns.org/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/dragolink.duckdns.org/privkey.pem;
#
#     location / {
#         proxy_pass http://backend:8080;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
